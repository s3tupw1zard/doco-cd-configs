services:
  obsidian-livesync:
    container_name: obsidian-livesync
    hostname: obsidian-livesync
    image: couchdb:3.5.2.1@sha256:9ea24cbd76522fe845d1c32c7fd1dcfc8a3ba73dcc4817d62f8a7f7f1dfaffe3
    environment:
      - COUCHDB_USER=${COUCHDB_USER}
      - COUCHDB_PASSWORD=${COUCHDB_PASSWORD}
    volumes:
      - /srv/tools/obsidian-livesync/couchdb/data:/opt/couchdb/data
      - /srv/tools/obsidian-livesync/couchdb/etc:/opt/couchdb/etc/local.d
    ports:
      - "5984:5984"
    restart: always
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${SERVICE_NAME}.rule=Host(`${SERVICE_DOMAIN}`)"
      - "traefik.http.routers.${SERVICE_NAME}.entrypoints=websecure"
      - "traefik.http.routers.${SERVICE_NAME}.service=${SERVICE_NAME}"
      - "traefik.http.routers.${SERVICE_NAME}.tls.certresolver=cloudflare"
      - "traefik.http.services.${SERVICE_NAME}.loadbalancer.server.port=5984"
      - "traefik.docker.network=traefik"

networks:
  traefik:
    external: true