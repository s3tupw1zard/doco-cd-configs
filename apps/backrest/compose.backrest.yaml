services:
  backrest:
    image: ghcr.io/garethgeorge/backrest:v1.14.1@sha256:b852979754281026230cc69fb11428e6d57c9a97784ab4a444ffc7934c53a215
    container_name: backrest
    hostname: backrest
    restart: unless-stopped
    volumes:
      - /srv/core/backrest/data:/data
      - /srv/core/backrest/config:/config
      - /srv/core/backrest/cache:/cache
      - /srv/core/backrest/tmp:/tmp
      - /srv/core/backrest/rclone:/root/.config/rclone # Mount for rclone config (needed when using rclone remotes)
      - /srv:/userdata
      #- /path/to/local/repos:/repos     # Mount local repos (optional for remote storage)
    environment:
      BACKREST_DATA: "/data"
      BACKREST_CONFIG: "/config/config.json"
      XDG_CACHE_HOME: "/cache"
      TMPDIR: "/tmp"
      TZ: "Europe/Berlin"
      BACKUP_USER: "${BACKUP_USER}"
      BACKUP_PASS: "${BACKUP_PASS}"
#    ports:
#      - "9898:9898"
    networks:
      - traefik
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.${SERVICE_NAME}.rule=Host(`${SERVICE_DOMAIN}`)"
      - "traefik.http.routers.${SERVICE_NAME}.entrypoints=websecure"
      - "traefik.http.routers.${SERVICE_NAME}.service=${SERVICE_NAME}"
      - "traefik.http.routers.${SERVICE_NAME}.tls.certresolver=cloudflare"
      #- "traefik.http.routers.${SERVICE_NAME}.middlewares="
      - "traefik.http.services.${SERVICE_NAME}.loadbalancer.server.port=9898"
      - "traefik.docker.network=traefik"

networks:
  traefik:
    external: true
